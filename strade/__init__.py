"""Italian Street Extractor package.

A command-line tool that reads an OpenStreetMap dump and produces a distinct
list of named streets, split into an ``extract`` and a ``join`` command that
hand off through a SQLite checkpoint database.
"""
